# Environment variables for the company LLM RAG system

# --- Jira Configuration ---
JIRA_BASE_URL='https://your-company.atlassian.net'
JIRA_API_TOKEN='YOUR_JIRA_API_TOKEN'
JIRA_EMAIL='your-email@example.com'
JIRA_PROJECT_KEY='CUPPING' # As per request

# --- Confluence Configuration ---
CONFLUENCE_BASE_URL='https://your-company.atlassian.net/wiki'
CONFLUENCE_API_TOKEN='YOUR_CONFLUENCE_API_TOKEN'
CONFLUENCE_EMAIL='your-email@example.com'
CONFLUENCE_SPACE_KEY='O2OLAB' # As per request

# --- MS365 (SharePoint & Teams) Configuration ---
# These require an Azure AD App Registration.
# See Microsoft Graph API documentation for details.
TENANT_ID='YOUR_AZURE_AD_TENANT_ID'
CLIENT_ID='YOUR_AZURE_AD_CLIENT_ID'
CLIENT_SECRET='YOUR_AZURE_AD_CLIENT_SECRET'
SHAREPOINT_SITE_NAME='o2olab group' # As per request
TEAMS_GROUP_NAME='Your Teams Group Name' # Example: 'My Development Team'

# --- OpenAI Configuration ---
OPENAI_API_KEY='YOUR_OPENAI_API_KEY'

# --- ChromaDB Configuration (Optional, defaults are usually fine) ---
# CHROMA_DB_PATH='./chroma_db'
# COLLECTION_NAME='company_llm_rag_collection'

# --- How to use these variables ---
# 1. Rename this file to .env
# 2. Fill in your actual credentials and desired values.
# 3. In your terminal, navigate to the project root directory.
# 4. Source this file before running any Python scripts:
#    source .env
#    OR, manually export each variable:
#    export JIRA_API_TOKEN='YOUR_JIRA_API_TOKEN'
#    ... and so on for all variables.
