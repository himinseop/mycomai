# 2026-07-05

## 도메인별 LLM 인사이트 API Phase 1~3 (#56, feature/56-insight-api)
- **구조**: `insight_api/` 패키지 — 도메인 레지스트리(`POST /api/v1/insights/{domain}`). 도메인 = 입력 스키마 + 통계 선계산 + 프롬프트 + 응답 파싱. **수치는 서버가 계산, LLM은 해석만**.
- **Phase 1**: X-API-Key 인증(SHA-256 해시 저장) + 도메인 scope + IP allowlist(CIDR), `sales` 도메인(총액/추이/차원별/전기대비/z-score 이상치), `api_call_history` 이력(원본 데이터 미저장), 키 발급 CLI(`scripts/api_key_issue.py`).
- **Phase 2**: 관리자 API 탭(키 발급 1회 노출 모달/차단/재활성, 호출 이력 필터 조회), 키별 분당 rate limit(sliding window, 429도 이력 기록).
- **Phase 3**: `voc` 도메인(평점 분포/부정 비율/카테고리 집계/건수 급증 감지 + 부정 우선 샘플로 LLM theme 도출)으로 **레지스트리 확장 구조 검증** — 파일 1개+등록+프롬프트만으로 도메인 추가, 공통 파이프라인 무변경. 샘플 원문은 응답/이력 비노출.
- **E2E에서 발견·수정**: gpt-4o-mini가 한글 금액 단위 변환 오류(1,605만→"1억 6050만") → 금액·증감률 표시 문자열(`*_display`)을 서버가 확정하고 프롬프트에서 그대로 쓰도록 강제. 재검증에서 수치 전부 정확.
- **검증**: 테스트 23건(설계 TC 1~12 + rate limit + voc) 통과, 실 LLM E2E(매출·VOC) 수기 검산 일치.

## 기타
- compose 프로젝트명 `knowledge-hub` 변경 후속: 검증용 API 키는 발급→검증→즉시 차단 처리.

## 인사이트 API 단일 엔드포인트 전환 (#56 설계 변경)
- 도메인별 경로 제거 → `POST /api/v1/insights` 하나로 통합. 서버가 데이터를 근거로 프롬프트 자동 선택.
- 선택: payload `domain` 명시 > 구조 감지(signature_fields 커버리지) > LLM 분류(질문+필드+샘플). 응답·이력에 `domain_selection` 기록.
- `question` 필드 신설(분류·해석 반영), `period` 생략 시 records 날짜로 추론, scope `*` 지원.
- 테스트 28건 통과, 실 LLM E2E 4시나리오(구조/VOC/명시/혼합→LLM) 검증.

## 어드민 개선: LLM 모델 스위치 · API 메뉴 통합 · 접속 이력 (2026-07-06)
- **LLM 모델 스위치**: 설정 탭에서 역할별(답변 생성/질문 재작성/요약·보조/인사이트 API) 모델을 셀렉트로 변경. `llm/factory.current_model(role)` — app_settings 오버라이드 > .env 기본값, 재시작 없이 즉시 반영. 화이트리스트 검증(422). ollama provider에서는 비활성.
- **API 메뉴 → 설정 탭 통합**: 별도 API 탭 제거, 설정 탭 안에 API 클라이언트·호출 이력 카드로 이동.
- **호출 이력 표시 깨짐 수정**: 원본 JSON 노출 → 읽기 좋은 요약(기간·행수·차원·선택방법·질문)으로 변환, UTC → 로컬 시각(fmtDate), 오류는 별도 줄, 테이블 가로 스크롤 래퍼.
- **접속 이력**: 채팅방(/)·어드민(/admin) 진입 시 IP·시각·User-Agent 기록(web_access_log, 30일 보관) → 대시보드 하단 카드로 조회. ⚠ Docker Desktop NAT 특성상 소스 IP가 192.168.65.1로 보임 — 실 IP는 앞단 프록시(XFF) 필요.

## 역할별 LLM provider 분리 스위치 (#38 후속, 2026-07-06)
- 설정 탭 LLM 카드에 역할별 **provider 셀렉트**(기본/openai/ollama) 추가 — `resolve_llm(role)`이 app_settings 오버라이드로 provider+모델을 런타임 결정. Ollama 연결 상태(🟢/🔴)를 설정 화면에 표시.
- qwen2.5:3b 역할별 실측: **rewrite 부적합**(6~12s 지연이 검색 전 크리티컬 패스 + "위메포" 오타 보정 실패, 키워드 1개), **summarize 사용 가능**(2.9s, 후속질문 품질 준수, 크리티컬 패스 밖).
- 기본값은 전 역할 openai 유지 — 전환은 관리자 스위치로 즉시 가능.

## Docker VM 5GB 축소 — Ollama용 호스트 RAM 확보 (2026-07-07)
- Docker Desktop 8→5GB(사용자), web `mem_limit` 12g→4g, data-loader 8g→4g, ChromaDB 캐시 4→2GB.
- 결과: anon 2.4GiB + file 1.3GiB (4g 한도 내 안정), **벡터 검색 370~530ms 유지**(keep-alive 유효).
- ⚠ 리랭커 6.4~7.1s로 저하 (이전 ~2-3s) — Docker VM CPU 4개 확인, 코어 수 영향 추정. CPU 재상향 후 재측정 필요.

## 리랭커 bge-base 전환 (2026-07-07)
- VM 5GB 축소 후 v2-m3(fp16) 리랭커가 5.1~5.5s로 병목(CPU 8 상향해도 소폭 개선). 
- **실사용 질문 9건 LLM-judge A/B**(scripts/ab_reranker.py, 블라인드 0~2점 NDCG@3): v2-m3 **1.000** vs base **0.985** — 품질 사실상 동등, 속도 **6.1배**(8.2s→1.3s/케이스).
- `.env`: RERANKER_MODEL=bge-reranker-base + RERANKER_DTYPE=float32 (CPU에선 fp32가 더 빠름).
- 프로덕션 결과: rerank **0.9~1.0s**, 검색 전체 1.2~1.6s(8GB 시절보다 빠름), web anon 2.4→**2.0GiB**.

## qwen2.5:7b 생성 품질 재검증 — 부적합 (2026-07-07)
- VM 5GB 축소로 확보한 RAM에서 7B 검증(scripts/ab_generation.py, 실사용 질문 8건, gpt-4o 블라인드 채점).
- **gpt-4o-mini 3.38점·할루 0/8·3.6s** vs **qwen2.5:7b 2.38점·할루 3/8·58.8s(max 90s)**. 우세 openai 4 / ollama 0 / tie 4.
- 결론: M1 16GB에서 7B는 지연(분당 1건)만으로도 사용 불가, 할루시네이션도 3b와 동일하게 발생 → **생성(chat) 역할 Ollama 전환 보류 유지**. sLLM 전환은 전용 하드웨어(GPU 서버/32GB+) 확보 시 재검토.

## Jira/Confluence 수집 정상화 + 전체 재수집 (2026-07-10)
- **자격증명 복구**: 스코프 토큰(401) → 클래식 토큰 재발급 + 발급 계정 이메일 짝 수정으로 해결.
- **Confluence 레이블 필터 버그 발견·수정**: v2 API `label`(단수) 무시로 개인 스페이스 포함 97개 전체 수집되던 것 → `labels`로 수정, `platform` 16개만 수집 (적재 전 중지로 오염 없음).
- **전체 재수집**: Jira 6프로젝트(확장) 25,152건 / Confluence 986 / SharePoint 4,188 / Teams 4,067. 적재 2시간 21분, 실패 0 — 청크 160,712 → **165,289** (+4,577 신규, 39,907 갱신).
- 부수 발견: recency 질의("최근 WPLUS 이슈")가 top-K 의미검색 한계로 최신 이슈를 못 찾는 구조 확인 → #59 GraphRAG P1 근거로 기록.

## ChromaDB 분리 운영 Stage 1 완료 (#60, 2026-07-11)
- **CHROMA_MODE 스위치**(embedded 기본/http) + `db_manager.health()`(8초 캐시) 구현. 대시보드 "🧠 ChromaDB 상태" 카드 + 채팅 접속 시 상태 표시(🟢/배너) 포함.
- **로컬 분리 전환**: chroma 1.4.1 서버 컨테이너(같은 db 디렉토리 마운트, 루프백 바인딩) + web http 모드. count 165,289 일치, 벡터 warm 529~736ms(+100~300ms 오버헤드), **web 메모리 2.9→1.2GiB**.
- **발견**: chroma 1.x Rust 서버는 서버측 토큰 인증 미지원 → Stage 2 배포 패키지(deploy/chroma-server)에 **nginx Bearer 검증 프록시** 포함. 클라이언트 TokenAuthClientProvider 헤더와 호환.
- **이관 준비물**: `scripts/backup_chroma.sh`(tar+sha256, 첫 백업 1.6GB 생성 완료), deploy/chroma-server(compose+프록시+복원 README). Stage 2는 .18 장비 Docker 준비 후 진행.

## ChromaDB .18 이관 완료 — Stage 2 (#60, 2026-07-14)
- .18(M1 32GB, we@wes-macbook-pro-m1) SSH 구축 → 사용자 측 백업 복원 + compose 기동 확인 → 검증: 무토큰 401(nginx 프록시), 토큰 heartbeat, **count 165,289 정확 일치**, LAN 왕복 20ms.
- 운영 전환: `.env` HOST=192.168.0.18/PORT=9000 → web 재생성 → E2E 벡터 warm 453~1003ms(로컬 분리와 동급), 로컬 chroma 중지(롤백용 데이터 보존).
- **web 메모리 989MiB** (embedded 시절 2.9GiB → 분리 1.2 → 원격 0.99). mem_limit 4g→3g 하향.
- .18 절전 꺼짐(sleep 0) 확인. 주기 백업은 .18에서 수행 (deploy/chroma-server/README.md).
- 롤백 경로: `.env` HOST=chroma(로컬 서버) 또는 CHROMA_MODE=embedded — 로컬 db/chroma_db 보존.

## 수집 후 로컬 동기화 백업 체계 (#60 후속, 2026-07-14)
- `scripts/sync_chroma_backup.sh`: 원격 chroma 정지(일관 스냅샷) → rsync로 로컬 `db/chroma_db` 미러링(embedded 롤백 데이터 상시 최신) → 재기동(trap 보장) → tar+sha256 백업 회전(최근 3개).
- `scripts/collect_and_sync.sh`: 수집 표준 진입점 — 적재 성공 시에만 동기화 백업 연쇄 실행.
- 실검증: 3.5G 동기화, 원격 재기동 정상(401), 백업 3개 회전 확인.

## 야간 자동 수집 launchd 스케줄 (2026-07-14)
- `deploy/launchd/com.mycomai.collect.plist` — 매일 02:00 `collect_and_sync.sh`(수집→.18 적재→로컬 동기화 백업) 실행. 로그 `~/Library/Logs/mycomai-collect.log`.
- 설치·기동 완료. 호스트 절전 0 확인. 컨테이너 cron(미기동)은 대체됨 — 호스트 launchd가 ssh/rsync 접근 가능해 동기화까지 일원화.

## LLM 위키 Phase 1 (#58, 2026-07-14)
- **구현**: `wiki/` 패키지 — wiki_store(wiki_pages 테이블+대표질문 임베딩 lifecycle), topic_miner(질문 로그 LLM 클러스터링), page_builder(하이브리드 검색 소스 수집→LLM 합성, 출처 인용 강제+팩트 블록 동시 추출=#59 시드). 관리자 "위키" 탭(후보 분석/생성/미리보기/승인·폐기·재생성).
- **서빙**: WIKI_RRF_BOOST(3.0, Hub 5.0보다 낮게) + 검색 결과에서 위키 질문 매칭 시 페이지 본문 컨텍스트 주입. disabled는 임베딩 제거+주입 차단. 재생성 시 draft 강등(재검수).
- **E2E**: 실 질문 로그에서 토픽 6개 도출 → "포인트 적립·사용 정책" 페이지 합성(소스 25건, 실제 파일명 인용, 팩트 3개) → 유사 질문 검색 1위+본문 주입 확인. 테스트 8건 + 전체 36건 통과.
- **발견**: 포인트 유효기간이 문서 간 상충(TC=100일 vs 기획서=1년) — 검수 게이트 필요성 실증, Phase 2 모순 검증 대상 사례.

## LLM 위키 Phase 2 (#58, 2026-07-14)
- **승인 페이지 직접 응답**: 1위=approved 위키 + RRF 2배 우세 시 원문 직접 반환(안내 멘트+기준일 표기, model=wiki_direct). Hub가 먼저 검사되어 우선순위 보장. draft는 계속 부스트만(검수 게이트).
- **신선도 자동 갱신**: data_loader 종료 훅 — 페이지별 소스 재검색→source_hash 비교→변경분만 재생성(draft 강등)+Teams 알림. 관리자 수동 "신선도 점검" 버튼도 추가.
- **팩트 모순 검증**: 페이지 간 동일 key·상이 value 교차 검사(공백/대소문자 정규화) → 위키 탭 상단 경고 카드. 온톨로지 커리큘럼에서 채택한 실용 추론 요소.
- UI: 설정·위키 패널 max-width 제거(대시보드와 동일 폭).
- E2E: 승인→"포인트 적립 방법" 질의→위키 원문 직접 응답 확인. 테스트 40건 통과(위키 12).
