# 2026-07-02

## 웹 서비스 장애 복구
- **원인**: `docker-web-1` 워커가 OOM-kill 후 좀비 상태로 남아 모든 HTTP 요청 행업. 컨테이너는 `Up`이나 응답 없음.
- **조치**: 컨테이너 재기동으로 즉시 복구. Docker Desktop 메모리 7.75GB → 12GB 상향.

## 메모리 최적화 (완료)
| 이슈 | 요약 | 효과 |
|---|---|---|
| #44 | Reranker fp16 + 시작 시 db-stats 워밍업 제거 + `--reload` 운영 분리 | 상시 10.3 → 4.78 GiB |
| #45 | ChromaDB segment cache LRU + 메모리 한도 4GB | 쿼리 스파이크 15GB → 안정 |
| #46 | 응답 후 gc + `malloc_trim`으로 힙 회수 | -0.5 GiB |
| #47 | web 컨테이너 `mem_limit: 12g` | 좀비 워커 차단(OOM 시 자동 재시작) |
| #48 | data_loader 배치 dedup/upsert + **초대형 콘텐츠 상한(1MB)** | 적재 OOM/행업 근본 해결 |
| #49 | data-loader `mem_limit: 8g` + SharePoint 파일 크기 상한(50MB) | 컨테이너 격리 |

- **적재 행업/OOM 근본 원인 규명**: SharePoint에 파싱 후 텍스트가 **153MB**에 달하는 엑셀 로우데이터(`2020년로우데이터.xlsx` 등)가 있어 tiktoken 인코딩이 CPU 100%로 멈춤. 파일 크기 상한(#49)은 파싱 전 크기만 보므로 못 잡음 → **콘텐츠 길이 상한(#48)**이 실질 방어.

## DB 재적재 (완료)
- 전체 재수집·재적재 완주: **375,315 → 409,522 청크**. web 일시 중지로 메모리 확보 후 배치 적재로 OOM 없이 완료.
- SharePoint 360,607(+33,625) / Teams 5,963(+582) 갱신. **Jira/Confluence는 인증 실패(계정 권한/403)로 기존 데이터 유지** — 별도 자격증명 조치 필요.

## 프로토타입 (기본 off, 안전 롤아웃)
- **#52 질문 재작성**: gpt-4o-mini로 질문을 검색 친화적 문장+키워드로 재작성 → 원문+해석문 멀티쿼리 검색. `QUERY_REWRITE_ENABLED`(기본 off). 검증: "위메포 정산 언제 되냐"→"위메프오 정산 주기".
- **#53 no-answer 오분류 수정**: 실답변 뒤에 hedge로 붙은 no-answer 문구를 `endswith`로 오분류하던 문제. `_finalize_answer`로 실질 콘텐츠 길이 기준 판정 + 꼬리 문구 제거. (사례: sess-jd47o8t id=327)
- **#38 Ollama 연동**: `LLM_PROVIDER=openai|ollama`(기본 openai). Ollama는 OpenAI 호환 `/v1`라 기존 SDK 재사용, `base_url`만 추가. **임베딩은 항상 OpenAI 유지**(인덱스 1536d 호환).

## 조사·분석 (커밋 외 판단 근거)
- **Ollama qwen2.5:3b A/B**: 주 답변 LLM로 **부적합** — 근거 약해도 확신에 찬 답변(환각), 오프토픽 거절 실패(가드레일), 레이턴시 이점도 없음. 로컬 LLM 필요 시 더 큰 모델 재평가.
- **38초 벡터검색 원인(sess-hctton4)**: OpenAI 임베딩이 아니라 **~33분 유휴 후 HNSW 인덱스(2.8GB) cold page-in**. 재현: cold 14.9s → warm 0.9s → 0.4s. 구조적 원인은 working set 6.6GB > VM MemAvailable 4.1GB + LRU 4GB 축출. → 개선안: **인덱스 keep-alive(P0)**, data-loader/web 동시 실행 금지(P1), reranker 지연(warm 8초, P2).
- **자체 임베딩 검토**: 가능하나 **전체 재임베딩 전제**. 권장 경로 = Ollama `bge-m3`(호스트 실행 → 컨테이너 메모리 부담 0, Metal 가속, 온프레미스). 이슈 #51과 병합.

## 오픈 이슈 / 후속
- 신규 생성: #48~#53. #50(web 상시 메모리 절감), #51(임베딩 차원/자체 임베딩)은 미착수.
- 후속 검토: 벡터 인덱스 keep-alive, reranker 지연 개선, Jira/Confluence 자격증명 갱신.
